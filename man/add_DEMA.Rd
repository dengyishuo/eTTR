% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_DEMA.R
\name{add_DEMA}
\alias{add_DEMA}
\title{Add Double Exponential Moving Average (DEMA)}
\usage{
add_DEMA(
  mkt_data,
  n = 10,
  v = 1,
  wilder = FALSE,
  ratio = NULL,
  append = TRUE,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format panel data frame or tibble. Must contain
columns \code{date}, \code{code}, and the required price columns.}

\item{n}{Integer. Look-back window. Defaults to \code{10}.}

\item{v}{Numeric. Volume factor in the range \code{[0, 1]}. When \code{v = 1}
(default) the result is a standard DEMA; values between 0 and 1 produce a
blended MA.}

\item{wilder}{Logical. If \code{TRUE}, use Wilder's EMA smoothing ratio.
Defaults to \code{FALSE}.}

\item{ratio}{Numeric. Custom smoothing ratio. Defaults to \code{NULL}.}

\item{append}{Logical. If \code{TRUE} (default), append new columns to
\code{mkt_data}. If \code{FALSE}, return only \code{date}, \code{code},
\code{name}, and the result columns.}

\item{output}{Character. \code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
The input data frame with an additional column \code{DEMA_<n>}
containing the double exponential moving average of \code{close}.
}
\description{
Computes a Double Exponential Moving Average for each asset in
a long-format panel data frame and appends the result as a new column
named \code{DEMA_<n>}.
}
\examples{
data(ettr_stocks)
result <- add_DEMA(ettr_stocks)
}
