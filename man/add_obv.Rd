% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_obv.R
\name{add_obv}
\alias{add_obv}
\title{Calculate and add On-Balance Volume (OBV) to stock data}
\usage{
add_obv(mktdata, append = TRUE)
}
\arguments{
\item{mktdata}{xts object containing OHLCV data downloaded via quantmod}

\item{append}{logical. If TRUE, add OBV column to mktdata; otherwise, return only OBV column. Default is TRUE.}
}
\value{
If \code{append} is \code{TRUE}, a tibble object with added OBV column.
If \code{FALSE}, a tibble object with only OBV column.
}
\description{
This function calculates OBV, a technical analysis momentum indicator
that uses volume flow to predict changes in stock price.
}
\details{
OBV is calculated by:
\enumerate{
\item If today's closing price is higher than yesterday's, then:
OBV = Previous OBV + Today's Volume
\item If today's closing price is lower than yesterday's, then:
OBV = Previous OBV - Today's Volume
\item If today's closing price equals yesterday's, then:
OBV = Previous OBV (no change)
}
}
\note{
Input data must contain closing prices (Close) and volume (Volume)
}
\examples{
# Load internal Tesla stock data
data(TSLA)
# Calculate and add OBV column to TSLA data
tsla_with_obv <- add_obv(TSLA)
head(tsla_with_obv)
# Calculate OBV and return only OBV column
tsla_obv <- add_obv(TSLA, append = FALSE)
head(tsla_obv)
}
\keyword{OBV,}
\keyword{On-Balance}
\keyword{Volume,}
\keyword{analysis,}
\keyword{quantmod}
\keyword{technical}
