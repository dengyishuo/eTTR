% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_hma.R
\name{add_hma}
\alias{add_hma}
\title{Calculate and add Hull Moving Averages (HMA) to stock data}
\usage{
add_hma(mktdata, periods = c(50, 200), append = TRUE)
}
\arguments{
\item{mktdata}{xts object containing OHLCV data downloaded via quantmod}

\item{periods}{numeric vector with 2-4 positive integers representing HMA periods}

\item{append}{logical. If TRUE, add HMA columns to mktdata; otherwise, return only HMA columns. Default is TRUE.}
}
\value{
If \code{append} is \code{TRUE}, a tibble object with added HMA columns.
If \code{FALSE}, a tibble object with only HMA columns.
}
\description{
This function calculates HMA, a highly efficient moving average
that reduces lag significantly while maintaining smoothness, making it ideal for
identifying trends and reversals.
}
\details{
HMA is calculated using a three-step process:
\enumerate{
\item Compute a WMA with period n
\item Compute a WMA with period n/2
\item Compute a WMA of the difference between the two previous WMAs with period sqrt(n)
}
}
\note{
Input data must contain closing prices (Close)
}
\examples{
# Load internal Tesla stock data
data(TSLA)
# Calculate and add HMA columns to TSLA data
tsla_with_hma <- add_hma(TSLA, periods = c(10, 20), append = TRUE)
head(tsla_with_hma)
# Return only HMA columns for TSLA
tsla_only_hma <- add_hma(TSLA, periods = c(10, 20), append = FALSE)
head(tsla_only_hma)
}
\keyword{Average,}
\keyword{HMA,}
\keyword{Hull}
\keyword{Moving}
\keyword{analysis,}
\keyword{quantmod}
\keyword{technical}
