% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_WMA.R
\name{add_WMA}
\alias{add_WMA}
\title{Add Weighted Moving Average (WMA)}
\usage{
add_WMA(
  mkt_data,
  n = 10,
  wts = 1:n,
  append = TRUE,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{A long-format panel data frame or tibble. Must contain
columns \code{date}, \code{code}, and the required price columns.}

\item{n}{Integer. Look-back window. Defaults to \code{10}.}

\item{wts}{Numeric vector of length \code{n}. Weights applied to each
observation in the window. Defaults to \code{1:n} (linearly increasing).}

\item{append}{Logical. If \code{TRUE} (default), append new columns to
\code{mkt_data}. If \code{FALSE}, return only \code{date}, \code{code},
\code{name}, and the result columns.}

\item{output}{Character. \code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
The input data frame with an additional column \code{WMA_<n>}
containing the weighted moving average of \code{close}.
}
\description{
Computes a Weighted Moving Average for each asset in a
long-format panel data frame and appends the result as a new column named
\code{WMA_<n>}.
}
\examples{
data(ettr_stocks)
result <- add_WMA(ettr_stocks)
}
