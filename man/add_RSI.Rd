% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_RSI.R
\name{add_RSI}
\alias{add_RSI}
\title{Add Relative Strength Index (RSI)}
\usage{
add_RSI(
  mkt_data,
  n = 14,
  maType,
  append = TRUE,
  output = c("tibble", "data.frame"),
  ...
)
}
\arguments{
\item{mkt_data}{A long-format panel data frame or tibble. Must contain
columns \code{date}, \code{code}, and the required price columns.}

\item{n}{Integer. Look-back window. Defaults to \code{14}.}

\item{maType}{Character or list. Moving average type used to smooth up and
down moves. Defaults to \code{"EMA"} (with Wilder smoothing).}

\item{append}{Logical. If \code{TRUE} (default), append new columns to
\code{mkt_data}. If \code{FALSE}, return only \code{date}, \code{code},
\code{name}, and the result columns.}

\item{output}{Character. \code{"tibble"} (default) or \code{"data.frame"}.}

\item{...}{Additional arguments passed to the moving average function.}
}
\value{
The input data frame with an additional column \code{RSI_<n>}
containing RSI values in the range \code{[0, 100]}.
}
\description{
Computes the Relative Strength Index for each asset in a
long-format panel data frame and appends the result as a new column named
\code{RSI_<n>}. RSI measures the speed and magnitude of price changes on
a 0-100 scale; readings above 70 are typically considered overbought and
below 30 oversold.
}
\examples{
\dontrun{
mkt_data <- data.frame(
  date  = rep(seq.Date(as.Date("2023-01-01"), by = "day", length.out = 60), 2),
  code  = rep(c("AAPL", "MSFT"), each = 60),
  name  = rep(c("Apple", "Microsoft"), each = 60),
  close = c(runif(60, 150, 200), runif(60, 300, 400))
)
# Example 1: Default parameters
result <- add_RSI(mkt_data)
# Example 2: Custom window
result <- add_RSI(mkt_data, n = 9)
# Example 3: Slim output
result <- add_RSI(mkt_data, n = 21, append = FALSE)
}
}
