% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_vwma.R
\name{add_vwma}
\alias{add_vwma}
\title{Calculate and add Volume Weighted Moving Averages (VWMA) to stock data}
\usage{
add_vwma(mktdata, periods = c(50, 200), append = TRUE)
}
\arguments{
\item{mktdata}{xts object containing OHLCV data downloaded via quantmod}

\item{periods}{numeric vector with 2-4 positive integers representing VWMA periods}

\item{append}{logical. If TRUE, add VWMA columns to mktdata; otherwise, return only VWMA columns. Default is TRUE.}
}
\value{
If \code{append} is \code{TRUE}, a tibble object with added VWMA columns.
If \code{FALSE}, a tibble object with only VWMA columns.
}
\description{
This function calculates VWMA, which weights each price by its corresponding
trading volume, giving more importance to price movements that occur during high volume periods.
}
\details{
VWMA is calculated by summing the product of each price and its volume over a specified period,
then dividing by the total volume over that period.
}
\note{
Input data must contain closing prices (Close) and volume (Volume)
}
\examples{
library(quantmod)
library(tibble)
library(zoo)
library(dplyr)
# Load internal Tesla stock data
data(TSLA)
# Calculate and add VWMA columns to TSLA data
tsla_with_vwma <- add_vwma(TSLA, periods = c(10, 20), append = TRUE)
head(tsla_with_vwma)
# Return only VWMA columns for TSLA
tsla_only_vwma <- add_vwma(TSLA, periods = c(10, 20), append = FALSE)
head(tsla_only_vwma)
}
\keyword{VWMA,}
\keyword{analysis,}
\keyword{quantmod}
\keyword{technical}
\keyword{volume}
\keyword{weighted,}
