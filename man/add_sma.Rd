% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_SMA.R
\name{add_SMA}
\alias{add_SMA}
\title{Add Simple Moving Average (SMA)}
\usage{
add_SMA(mkt_data, n = 10, append = TRUE, output = c("tibble", "data.frame"))
}
\arguments{
\item{mkt_data}{A long-format panel data frame or tibble. Must contain
columns \code{date}, \code{code}, and the required price columns.}

\item{n}{Integer. Look-back window. Defaults to \code{10}.}

\item{append}{Logical. If \code{TRUE} (default), append new columns to
\code{mkt_data}. If \code{FALSE}, return only \code{date}, \code{code},
\code{name}, and the result columns.}

\item{output}{Character. \code{"tibble"} (default) or \code{"data.frame"}.}
}
\value{
The input data frame with an additional column \code{SMA_<n>}
containing the simple moving average of \code{close}.
}
\description{
Computes a Simple Moving Average over a rolling look-back window
for each asset in a long-format panel data frame and appends the result as
a new column named \code{SMA_<n>}.
}
\examples{
data(ettr_stocks)
result <- add_SMA(ettr_stocks)
}
