% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_rollSFM.R
\name{add_rollSFM}
\alias{add_rollSFM}
\title{Add Rolling Single-Factor Model (Panel Data)}
\usage{
add_rollSFM(
  mkt_data,
  Rb,
  n = 60,
  append = TRUE,
  output = c("tibble", "data.frame")
)
}
\arguments{
\item{mkt_data}{Panel with date, code, and asset returns column (e.g., ret_asset).}

\item{Rb}{xts of factor returns (must have dates aligning with mkt_data dates). Could be a single series for all assets.}

\item{n}{Rolling window size. Default 60.}

\item{append}{Logical.}

\item{output}{"tibble" or "data.frame".}
}
\value{
Data frame with columns alpha, beta, r.squared.
}
\description{
Add Rolling Single-Factor Model (Panel Data)
}
\examples{
\dontrun{
# add_rollSFM requires a pre-computed ret_asset column and an xts benchmark
data(ettr_stocks)
df <- ettr_stocks
df$ret_asset <- ave(df$close, df$code,
                    FUN = function(x) c(NA, diff(x) / head(x, -1)))
Rb <- xts::xts(df$ret_asset[df$code == df$code[1]],
               order.by = df$date[df$code == df$code[1]])
result <- add_rollSFM(df, Rb = Rb, n = 60)
}
}
