% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stoch.R
\name{stoch}
\alias{stoch}
\title{Stochastic Oscillator}
\usage{
stoch(
  HLC,
  nFastK = 14,
  nFastD = 3,
  nSlowD = 3,
  maType,
  bounded = TRUE,
  smooth = 1,
  ...
)
}
\arguments{
\item{HLC}{Object coercible to xts or matrix, containing High-Low-Close prices
or a univariate series.}

\item{nFastK}{Number of periods for fast \%K calculation (default: 14).}

\item{nFastD}{Number of periods for fast \%D calculation (default: 3).}

\item{nSlowD}{Number of periods for slow \%D calculation (default: 3).}

\item{maType}{Moving average type: function, string, or list of configurations
(default: "SMA").}

\item{bounded}{Logical, use current period in range calculation (default: TRUE).}

\item{smooth}{Internal smoothing periods for FastK (default: 1).}

\item{...}{Additional arguments passed to moving average functions.}
}
\value{
An xts or matrix object with columns: fastK, fastD, slowD.
}
\description{
Calculates the fast \%K, fast \%D, and slow \%D values of
the stochastic oscillator.
}
\examples{
data(ttrc)
stoch_values <- stoch(ttrc[, c("High", "Low", "Close")])
plot(tail(stoch_values[, "fastK"], 100),
  type = "l",
  main = "Fast \%K of Stochastic Oscillator", ylab = "\%K"
)

}
\author{
DengYishuo
}
\keyword{indicator}
\keyword{momentum}
\keyword{ts}
